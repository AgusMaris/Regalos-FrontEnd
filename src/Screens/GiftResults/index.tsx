import { View, Text } from 'react-native'
import React from 'react'

type Props = {
  setAuth: () => void
}

const GiftResultsScreen = (props: Props) => {
  return (
    <View>
      <Text>GiftResultsScreens</Text>
    </View>
  )
}

export default GiftResultsScreen
